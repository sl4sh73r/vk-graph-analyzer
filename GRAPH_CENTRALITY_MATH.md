# Теория: графы и метрики центральности

Документ объясняет основы теории графов и метрик центральности, которые используются в проекте. Приведены формулы, визуальные схемы и небольшой пример расчётов.

## Базовые определения

- Граф: G = (V, E), где V — множество вершин, E — множество рёбер
- Неориентированный граф: (u, v) = (v, u)
- Соседи узла v: N(v) = { u ∈ V | (u, v) ∈ E }
- Степень узла v: deg(v) = |N(v)|
- Кратчайшее расстояние d(u, v) — длина кратчайшего пути между u и v
- Матрица смежности A: A_{ij} = 1, если (i, j) ∈ E, иначе 0

## Наглядный пример графа

```mermaid
graph LR
  A((A)) -- 1 -- B((B))
  A((A)) -- 1 -- C((C))
  B((B)) -- 1 -- C((C))
  B((B)) -- 1 -- D((D))
  C((C)) -- 1 -- E((E))
```

Матрица смежности A для примера (порядок вершин A, B, C, D, E):

|   | A | B | C | D | E |
|---|---|---|---|---|---|
| A | 0 | 1 | 1 | 0 | 0 |
| B | 1 | 0 | 1 | 1 | 0 |
| C | 1 | 1 | 0 | 0 | 1 |
| D | 0 | 1 | 0 | 0 | 0 |
| E | 0 | 0 | 1 | 0 | 0 |

## Метрики центральности

Ниже — определения используемых и часто встречающихся метрик. В приложении считаются closeness, betweenness, eigenvector и кастомная rooted.

### Degree centrality

- Формула: C_D(v) = deg(v) / (|V| - 1)
- Идея: узел важен, если у него много связей
- В проекте эта метрика не используется напрямую, но полезна как интуитивная база

### Closeness centrality

- Классическое определение: C_C(v) = (|V| - 1) / \(\sum_{u \neq v} d(v, u)\)
- Идея: узел важен, если находится "близко" ко всем остальным
- В проекте используется реализация из graphology, затем применяется min-max нормализация к диапазону [0, 1]

### Betweenness centrality

- Определение: C_B(v) = \(\sum_{s \neq v \neq t} \frac{\sigma_{st}(v)}{\sigma_{st}}\)  
  где \(\sigma_{st}\) — число кратчайших путей между s и t, а \(\sigma_{st}(v)\) — число таких путей, проходящих через v
- Идея: узел важен, если он часто лежит на кратчайших путях между другими
- В проекте используется реализация Brandes из graphology, затем min-max нормализация

### Eigenvector centrality

- Определение как собственный вектор: A x = \(\lambda\) x  
  Центральность узла пропорциональна сумме центральностей его соседей
- Итеративно: x^{(k+1)} = A x^{(k)} / \|A x^{(k)}\|
- В проекте используется реализация из graphology, затем min-max нормализация

### PageRank (для справки)

- Не используется в проекте, но близок к eigenvector с демпфированием
- Формула стационарного распределения: p = \(\alpha P^T p + (1-\alpha) v\), где P — стохастическая матрица переходов

### Rooted centrality (используется в проекте)

- Кастомная метрика "личной важности" относительно выбранного корня r
- Пусть maxD — максимальное расстояние от r до достижимых вершин  
  Тогда:

$$
C_{rooted}(v \mid r) = \begin{cases}
1, & v = r \\
1 - \frac{d(r, v)}{\max D}, & v \text{ достижим и } v \neq r \\
0, & v \text{ недостижим}
\end{cases}
$$

- Вычисляется через BFS от r по неориентированному графу

### Нормализация результатов в приложении

Для метрик closeness, betweenness и eigenvector после вычисления применяется min-max нормализация по всем узлам:

$$
\widehat{x}_i = \begin{cases}
0.5, & \text{если } \max(x) - \min(x) = 0 \\
\dfrac{x_i - \min(x)}{\max(x) - \min(x)}, & \text{иначе}
\end{cases}
$$

Это соответствует коду в `backend/src/utils/centrality.ts` и гарантирует значения в диапазоне [0, 1].

## Маленький пример расчёта

Возьмём граф выше и корень r = A.

### Расстояния от A

- d(A, A) = 0
- d(A, B) = 1
- d(A, C) = 1
- d(A, D) = 2 \(A \to B \to D\)
- d(A, E) = 2 \(A \to C \to E\)

Сумма расстояний от A до остальных: 1 + 1 + 2 + 2 = 6

### Closeness для A

$$
C_C(A) = \frac{|V| - 1}{\sum_{u \neq A} d(A, u)} = \frac{4}{6} \approx 0.667
$$

После вычисления всех C_C(v) по узлам графа, в приложении значения будут дополнительно нормированы в [0, 1] по формуле выше.

### Rooted centrality при r = A

- maxD = 2 (самое далёкое расстояние от A)
- C_rooted(A) = 1
- C_rooted(B) = 1 - 1/2 = 0.5
- C_rooted(C) = 1 - 1/2 = 0.5
- C_rooted(D) = 1 - 2/2 = 0
- C_rooted(E) = 1 - 2/2 = 0

## Соответствие реализации в проекте

- Файл `backend/src/utils/centrality.ts`
  - Использует библиотеку graphology для: closeness, betweenness, eigenvector
  - Преобразует граф друзей в неориентированный (addUndirectedEdge)
  - Добавляет метрику rooted через BFS и формулу 1 − d/maxD
  - Применяет min-max нормализацию для сопоставимости шкал

## Полезные ресурсы

- Freeman, L. C. (1977). A set of measures of centrality based on betweenness
- Newman, M. E. J. (2010). Networks: An Introduction
- Brandes, U. (2001). A faster algorithm for betweenness centrality
- Документация graphology metrics: https://graphology.github.io/standard-library/metrics.html
